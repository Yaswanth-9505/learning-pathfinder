# Streamlit deployment dependencies.
# Streamlit Community Cloud installs exactly this file, so it stays minimal —
# the FastAPI stack lives in requirements-api.txt and is not needed here.
streamlit>=1.62.0
groq>=1.6.0
python-dotenv>=1.0.0
